{
  // Enable debug mode.
  "debug": false,

  // Rubocop command.
  // You may point `rubocop` binary to a different path if you need to.
  // By default, this will be inferred automatically from $PATH.
  "command": ["rubocop", "--show-cops"]
}
